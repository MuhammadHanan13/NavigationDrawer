/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RouteDrawer from './RouteDrawer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RouteDrawer);
